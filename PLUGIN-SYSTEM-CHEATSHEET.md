# lite-fsm — Plugin System Cheat Sheet

Plugin system public API находится на финализации.

Старый authoring API с пользовательским `install(ctx)` не является целевым public API. Актуальная документация будет создана после завершения `definePlugin().create(...)` и связанного storage DSL.
