# Журнал реализации ТЗ `@lite-fsm/entities` reducer layer

ТЗ: [`reducer-layer-performance-spec.md`](./reducer-layer-performance-spec.md)

Цель журнала — восстановить состояние реализации после сжатия контекста. Журнал не заменяет ТЗ и не пересказывает diff.

## Правила ведения

- Перед продолжением работы прочитать ТЗ и этот журнал.
- Продолжать первый этап со статусом не `done`.
- Не переходить к следующему этапу, пока текущий этап не прошел свой `stage gate`.
- Обновлять журнал после закрытия этапа, blocker или значимого промежуточного результата.
- Записи держать короткими: scope, измененные модули, проверки, coverage, риски, следующее действие.
- Не фиксировать каждую мелкую правку и не вставлять полный diff.

## Статусы

- `not started`
- `in progress`
- `done`
- `blocked`

## Текущий указатель

- Активное ТЗ: `packages/entities/reducer-layer-performance-spec.md`
- Активный этап: Финальная проверка
- Статус: `done`
- Следующее действие: цель завершена; передать итог пользователю.

## Сводка по этапам

| Этап | Название | Статус | Последнее обновление |
| ---- | -------- | ------ | -------------------- |
| 1 | Reducer diagnostics breakdown | `done` | 2026-06-15 |
| 2 | Compile-time reduce plans | `done` | 2026-06-15 |
| 3 | Stable reducer self и default transition fast path | `done` | 2026-06-15 |
| 4 | Single-pass post-processing и dirty rows | `done` | 2026-06-15 |
| 5 | Benchmarks, docs и performance report | `done` | 2026-06-15 |
| 6 | Рефакторинг, чистка и полировка | `done` | 2026-06-15 |

## Ход реализации

### Этап 1 — Reducer diagnostics breakdown

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` пустой; `git diff` пустой; `git diff --staged` пустой. Active scope исполнителя: `tests/bench/entities/diagnostics.fixture.mjs` и benchmark artifacts для smoke record/compare, если команда их создаст. Runtime `packages/entities/src/runtime/*`, public docs и cheatsheets вне scope этапа. Исполнитель: `019ec824-5946-7a92-be4a-f9ec8b858dec`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 1.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec824-5946-7a92-be4a-f9ec8b858dec`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `tests/bench/entities/diagnostics.fixture.mjs` выделяет reducer pipeline helper phases и добавляет diagnostics layers `collect/default transitions`, `create reducer self`, `run user reducer`, `validate final states`, `mark rows/public slice`, `schedule lifecycle work`, `update state buckets` для `movement update`, `projectile lifetime update`, `sprite sync reaction`; runtime, docs, cheatsheets и public API не менялись. Smoke artifacts созданы командами record как ignored `.bench` файлы: `reducer-breakdown-smoke.*`, `reducer-breakdown-50k-verify.*`, `latest.*`. Проверки: `pnpm run bench:entities:record -- --runs 1 --label reducer-breakdown-smoke --include diagnostics --row-counts 1000` — pass; `pnpm run bench:entities:compare -- .bench/entities/reducer-breakdown-smoke.json .bench/entities/reducer-breakdown-smoke.json` — pass; `pnpm run bench:entities:record -- --runs 1 --label reducer-breakdown-50k-verify --include diagnostics --row-counts 50000` — pass; `git diff --check` — pass. Baseline values 50 000: movement reducer layers sum `2.012ms` vs raw entity kernel `0.874ms` (`230.2%`): collect/default `0.459ms`, create self `0.001ms`, user reducer `0.516ms`, validate `0.411ms`, mark/public `0.322ms`, lifecycle `0.184ms`, buckets `0.118ms`; projectile sum `0.962ms` vs raw `0.646ms` (`149.1%`): collect/default `0.115ms`, create self `0.001ms`, user reducer `0.429ms`, validate `0.105ms`, mark/public `0.039ms`, lifecycle `0.187ms`, buckets `0.086ms`; sprite reducer sum `1.216ms` vs reduce entity batches `1.199ms` (`101.4%`). Coverage не применимо: fixture-only benchmark изменение без runtime behavior. Риски: 50k record имеет timer-noise warnings по raw SoA lower bound; stage gate не блокирует, потому что required reducer breakdown rows и 80% raw-kernel coverage закрыты. Следующее действие: этап 2.

### Этап 2 — Compile-time reduce plans

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` содержит принятые изменения этапа 1 и журнал (`M packages/entities/reducer-layer-performance-spec-log.md`, `M tests/bench/entities/diagnostics.fixture.mjs`); `git diff --stat`: log и diagnostics fixture, `203 insertions`, `33 deletions`; `git diff --staged` пустой. Active scope исполнителя: `packages/entities/src/runtime/compile.ts` и ближайшие internal runtime type files при необходимости; focused runtime tests в `tests/entities/entities-plugin.test.ts` или новом соседнем test file; public type tests только если появится type-visible contract. Out-of-scope baseline: изменения этапа 1 в diagnostics fixture и журнале не трогать. Исполнитель: `019ec82d-2dec-73d2-979c-08a67aec820b`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 2.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec82d-2dec-73d2-979c-08a67aec820b`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `packages/entities/src/runtime/compile.ts` добавляет internal `EntityReducePlan` и `reducePlansByEventCode` в `EntityTemplateMetadata`, индексированный по `eventCode`; новый `tests/entities/entities-compile-metadata.test.ts` покрывает identity, multi-state identity, non-identity, effects target, `despawnOn`, reactions, terminal target и lifecycle `ENTITY_SPAWNED`/`ENTITY_DESPAWNED` plans. `reduceAcceptedBatch`, reducer self, diagnostics fixture, public docs, cheatsheets и public exports не менялись. Проверки: `pnpm exec vitest run tests/entities/entities-compile-metadata.test.ts` — pass (`8` tests); `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-compile-metadata.test.ts --coverage --coverage.include=packages/entities/src/runtime/compile.ts` — pass (`153` tests), coverage `100%` statements/branches/functions/lines для `compile.ts`; `pnpm run check-types` — pass (`7` package checks, Tstyche `44` files / `492` tests); `git diff --check` — pass. Риск: `hasDespawnOnStates` является template-level flag, а не event-specific proof of entering despawn state; это соответствует этапу 2 и должно комбинироваться с final state checks в будущих runtime этапах. Следующее действие: этап 3.

### Этап 3 — Stable reducer self и default transition fast path

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` содержит принятые изменения этапов 1–2 и журнал (`M packages/entities/reducer-layer-performance-spec-log.md`, `M packages/entities/src/runtime/compile.ts`, `M tests/bench/entities/diagnostics.fixture.mjs`, `?? tests/entities/entities-compile-metadata.test.ts`); `git diff --stat`: log, `compile.ts`, diagnostics fixture (`302 insertions`, `36 deletions`, без untracked test file); `git diff --staged` пустой. Active scope исполнителя: `packages/entities/src/runtime/state.ts`, `packages/entities/src/runtime/reduce.ts`, при необходимости соседние internal runtime modules for reducer self cache invalidation; focused runtime tests в `tests/entities/entities-plugin.test.ts` или новом test file. Out-of-scope baseline: изменения этапов 1–2, public docs/cheatsheets и diagnostics fixture не трогать. Исполнитель: `019ec833-cf18-70d0-a716-5394d00affae`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 3.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec833-cf18-70d0-a716-5394d00affae`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `state.ts` добавляет stable cached reducer self на `ColumnarActorStore`, `getActorReducerSelf` и `rebindActorReducerSelf`; `reduce.ts` использует cached self, rebind при rollback restore и reduce-plan fast path для unscoped single-state identity default transition; `snapshot.ts` rebind cached self после hydrate commit; новый `tests/entities/entities-reducer-self.test.ts` покрывает default transition before reducer, rollback через `prevStateCode`, override через `self.states`, актуальные columns после capacity growth/hydrate/rollback и отсутствие `Object.entries(store.columns)` на hot identity `TICK`. Post-processing loops, reaction/effect/despawn scheduling, diagnostics fixture, public docs/cheatsheets и public exports не менялись. Проверки: `pnpm exec vitest run tests/entities/entities-reducer-self.test.ts` — pass (`9` tests); `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-reducer-self.test.ts --coverage --coverage.include=packages/entities/src/runtime/state.ts --coverage.include=packages/entities/src/runtime/reduce.ts --coverage.include=packages/entities/src/runtime/snapshot.ts` — pass (`154` tests), coverage `100%` statements/branches/functions/lines; `pnpm run check-types` — pass; `pnpm run lint` — pass; `git diff --check` — pass. Риски: added `dehydrate` assertion in reducer self test covers existing snapshot branch needed by full-module coverage; не меняет runtime contract. Следующее действие: этап 4.

### Этап 4 — Single-pass post-processing и dirty rows

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` содержит принятые изменения этапов 1–3 и журнал (`M packages/entities/reducer-layer-performance-spec-log.md`, `M packages/entities/src/runtime/compile.ts`, `M packages/entities/src/runtime/reduce.ts`, `M packages/entities/src/runtime/snapshot.ts`, `M packages/entities/src/runtime/state.ts`, `M tests/bench/entities/diagnostics.fixture.mjs`, `?? tests/entities/entities-compile-metadata.test.ts`, `?? tests/entities/entities-reducer-self.test.ts`); `git diff --stat`: log, `compile.ts`, `reduce.ts`, `snapshot.ts`, `state.ts`, diagnostics fixture (`415 insertions`, `77 deletions`, без untracked tests); `git diff --staged` пустой. Active scope исполнителя: `packages/entities/src/runtime/reduce.ts` и focused runtime tests in `tests/entities/entities-plugin.test.ts` or a new neighboring test file. Out-of-scope baseline: изменения этапов 1–3, public docs/cheatsheets и diagnostics fixture не трогать. Исполнитель: `019ec83c-92b9-7582-b268-9983186c8c69`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 4.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec83c-92b9-7582-b268-9983186c8c69`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `packages/entities/src/runtime/reduce.ts` сводит validation, `rowVersion`, dirty rows, effects, `despawnOn`, terminal scheduling и reaction ownership flags в один post-processing pass по accepted rows; `updateActorStateBuckets` получает только dirty rows; old full-scan helpers `acceptedRowsUseIdentityTransitions`, `acceptedRowsStayedInSourceState`, `acceptedRowsNeedDespawnOn` удалены; новый `tests/entities/entities-reducer-post-processing.test.ts` покрывает hot identity no `Set`/`Map`/column enumeration/full bucket scan и dirty-only bucket update after reducer override. Existing focused tests покрывают invalid stateCode, rowVersion, effects final state, `despawnOn`, `ENTITY_DESPAWNED`, terminal cleanup и multi-bucket order. Public docs/cheatsheets/API, diagnostics fixture, core bucket runtime и spawn allocation не менялись. Проверки: `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-reducer-self.test.ts tests/entities/entities-reducer-post-processing.test.ts` — pass (`156` tests); `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-reducer-self.test.ts tests/entities/entities-reducer-post-processing.test.ts --coverage --coverage.include=packages/entities/src/runtime/reduce.ts` — pass, `reduce.ts` coverage `100%` statements/branches/functions/lines; `pnpm run check-types` — pass; `pnpm run lint` — pass; `git diff --check` — pass. Риск: `reduce.ts` остается крупным файлом; split/refactor отложен на обязательный этап 6. Следующее действие: этап 5.

### Этап 5 — Benchmarks, docs и performance report

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` содержит принятые изменения этапов 1–4 и журнал (`M packages/entities/reducer-layer-performance-spec-log.md`, `M packages/entities/src/runtime/compile.ts`, `M packages/entities/src/runtime/reduce.ts`, `M packages/entities/src/runtime/snapshot.ts`, `M packages/entities/src/runtime/state.ts`, `M tests/bench/entities/diagnostics.fixture.mjs`, `?? tests/entities/entities-compile-metadata.test.ts`, `?? tests/entities/entities-reducer-post-processing.test.ts`, `?? tests/entities/entities-reducer-self.test.ts`); `git diff --stat`: log, `compile.ts`, `reduce.ts`, `snapshot.ts`, `state.ts`, diagnostics fixture (`544 insertions`, `165 deletions`, без untracked tests); `git diff --staged` пустой. Active scope исполнителя: `.bench/entities/after-reducer-layer.*`, `.bench/entities/after-reducer-layer-vs-after-reaction-scope.md`, optional `.bench/entities/after-reducer-layer-vs-codex-baseline-2026-06-14.md`, `packages/entities/PERFORMANCE.md`; benchmark record commands may update `.bench/entities/latest.*`. Out-of-scope baseline: runtime/test changes stages 1–4 and journal не трогать. Исполнитель: `019ec845-f056-77d1-b6d3-88c52f7998df`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 5.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec845-f056-77d1-b6d3-88c52f7998df`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `packages/entities/PERFORMANCE.md` добавляет раздел `Итог problem 3: reducer layer`; ignored `.bench` artifacts созданы/обновлены на диске: `after-reducer-layer.json/.md`, `after-reducer-layer-vs-after-reaction-scope.md`, `after-reducer-layer-vs-codex-baseline-2026-06-14.md`, `reducer-layer-smoke.json/.md`, `latest.json/.md` и timestamped copies. Runtime behavior, tests, public API, benchmark budgets, README и cheatsheets не менялись. Проверки: `pnpm run bench:entities:record -- --runs 1 --label reducer-layer-smoke --include gate,diagnostics --row-counts 1000` — pass по отчету исполнителя; `pnpm run bench:entities:record -- --runs 3 --label after-reducer-layer --include gate,diagnostics` — pass по artifacts (`runs=3`, include `gate,diagnostics`); `pnpm run bench:entities:compare -- .bench/entities/after-reaction-scope.json .bench/entities/after-reducer-layer.json` — pass, rerun by orchestrator; `pnpm run bench:entities:compare -- .bench/entities/codex-baseline-2026-06-14.json .bench/entities/after-reducer-layer.json` — pass, rerun by orchestrator; artifact consistency script — pass; `git diff --check` — pass. 50 000 gate after: movement `0.471ms`, `2.04x`, RSD `1.9%`; projectile `0.411ms`, `2.28x`, RSD `5.6%`; despawnOn cleanup `0.508ms`, `1.81x`, RSD `1.9%`; sprite sync reaction `3.093ms`, `6.32x`, RSD `5.2%`. Neighbor public gate regression > `10%` относительно `after-reaction-scope` отсутствует: все gate medians faster. Residual blocker для полного first gate documented in `PERFORMANCE.md`: `sprite sync reaction / 50 000 / reduce entity batches` `1.374ms` > `1.0ms`, `raw entity kernel` `2.173ms` > `2.0ms`; reducer-only production path ниже `0.5ms`, но diagnostics raw kernel model остается stale (`movement` `0.888ms`, `projectile` `0.810ms` быстрее public path не отражает). Coverage не применимо: docs/bench stage без code changes. Запрещенные docs build команды не запускались. Следующее действие: этап 6.

### Этап 6 — Рефакторинг, чистка и полировка

Статус: `done`

Записи:

- 2026-06-15 — baseline перед dispatch: `git status --short` содержит принятые изменения этапов 1–5 и журнал (`M packages/entities/PERFORMANCE.md`, `M packages/entities/reducer-layer-performance-spec-log.md`, `M packages/entities/src/runtime/compile.ts`, `M packages/entities/src/runtime/reduce.ts`, `M packages/entities/src/runtime/snapshot.ts`, `M packages/entities/src/runtime/state.ts`, `M tests/bench/entities/diagnostics.fixture.mjs`, `?? tests/entities/entities-compile-metadata.test.ts`, `?? tests/entities/entities-reducer-post-processing.test.ts`, `?? tests/entities/entities-reducer-self.test.ts`); `git diff --stat`: `PERFORMANCE.md`, log, runtime files and diagnostics fixture (`618 insertions`, `168 deletions`, без untracked tests); `git diff --staged` пустой. Active scope исполнителя: cleanup/refactor only in changed runtime modules and focused tests/docs from stages 1–5; `reduce.ts` currently `907` lines, so split into internal owner modules or reduce below `800` lines without behavior changes. Out-of-scope: benchmark budgets, new optimizations beyond reducer layer, public API changes, docs build. Исполнитель: `019ec851-9821-7a83-b82d-0b7f24562b40`; corrective: `0/3`; stage-owned delta: pending; смены исполнителя нет. Следующее действие: отправить self-contained brief этапа 6.
- 2026-06-15 — review gate `pass`, verify gate `accept`. Исполнитель: `019ec851-9821-7a83-b82d-0b7f24562b40`; corrective: `0/3`; смены исполнителя нет. Stage-owned delta: `packages/entities/src/runtime/mutation-snapshot.ts` выделяет snapshot/restore rollback mutation state из `reduce.ts`; `reduce.ts` сокращен до `742` строк и оставляет orchestration/default transitions/post-processing; `state.ts` экспортирует internal owner `rebuildActorAcceptStateBuckets`; `PERFORMANCE.md` убирает устаревшее problem 3 упоминание `createReducerSelf` как runtime target; `tests/entities/entities-reducer-post-processing.test.ts` добавляет узкую regression-проверку no-op bucket move. Поведение, public API, benchmark budgets и cheatsheets не менялись. Проверки: `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-reducer-self.test.ts tests/entities/entities-reducer-post-processing.test.ts --coverage --coverage.include=packages/entities/src/runtime/reduce.ts --coverage.include=packages/entities/src/runtime/state.ts --coverage.include=packages/entities/src/runtime/mutation-snapshot.ts --coverage.reporter=text` — pass (`157` tests), coverage `100%` statements/branches/functions/lines; `pnpm run check-types` — pass (`7` package checks, Tstyche `44` files / `492` tests); `pnpm run lint` — pass; `git diff --check` — pass. Source audit по stale helper/debug tokens не нашел runtime/test/doc hits; ожидаемые остатки только `createReducerSelf` в `tests/bench/entities/diagnostics.fixture.mjs` как benchmark layer helper и `console.log` в benchmark CLI scripts. Запрещенные docs build команды не запускались. Следующее действие: финальная проверка.

## Финальная проверка

- Статус: `done`
- Записи:
  - 2026-06-15 — старт финального readiness gate после закрытия этапов 1–6. Проверить интегральные runtime tests, strict coverage по измененным runtime-модулям, `check-types`, `lint`, `git diff --check`, source audit и непротиворечивость benchmark artifacts/performance summary.
  - 2026-06-15 — readiness gate `accept`. Проверки: `pnpm exec vitest run tests/entities/entities-plugin.test.ts tests/entities/entities-compile-metadata.test.ts tests/entities/entities-reducer-self.test.ts tests/entities/entities-reducer-post-processing.test.ts --coverage --coverage.include=packages/entities/src/runtime/compile.ts --coverage.include=packages/entities/src/runtime/reduce.ts --coverage.include=packages/entities/src/runtime/state.ts --coverage.include=packages/entities/src/runtime/snapshot.ts --coverage.include=packages/entities/src/runtime/mutation-snapshot.ts --coverage.reporter=text` — pass (`4` files, `165` tests), coverage `100%` statements/branches/functions/lines (`1232/1232`, `510/510`, `196/196`, `1065/1065`); `pnpm run check-types` — pass (`7` package checks, Tstyche `44` files / `492` tests); `pnpm run lint` — pass; `git diff --check` — pass; source audit по stale helper/debug tokens в runtime/tests/docs — clean; artifact consistency script for `.bench/entities/after-reducer-layer.*` and compare markdown — pass. `git ls-files` для generated `.bench` artifacts пустой: отчеты существуют на диске как ignored artifacts. Ожидаемые audit hits вне runtime scope: `createReducerSelf` в `tests/bench/entities/diagnostics.fixture.mjs` как diagnostics layer helper; `console.log` в benchmark CLI entrypoints. Запрещенные docs build команды не запускались.
  - 2026-06-15 — итоговый residual risk принят и задокументирован в `PERFORMANCE.md`: reducer-only production path для `50 000` строк ниже `0.5ms` (`movement` `0.471ms`, `projectile` `0.411ms`), соседних public gate-регрессий выше `10%` нет, но полный first gate не закрыт из-за `sprite sync reaction / reduce entity batches` `1.374ms` > `1.0ms`, `sprite sync reaction / raw entity kernel` `2.173ms` > `2.0ms` и stale diagnostics model для reducer-only raw kernel. Public API/types не менялись; `API-CHEATSHEET.md` и `TYPES-CHEATSHEET.md` не требуют обновления.
